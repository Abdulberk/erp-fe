import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // Geliştirme rozeti ekran görüntülerinin köşesine giriyor.
  devIndicators: false,
};

export default nextConfig;
